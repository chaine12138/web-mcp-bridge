{
  "name": "web-mcp-shared",
  "version": "0.1.0",
  "type": "module",
  "description": "Shared protocol types, constants and zod schemas used by web-mcp-bridge and web-mcp-sdk.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}